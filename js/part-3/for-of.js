// for of statement     - iterating arrays and collections
console.log("\nTesting for-of loop:");

arr = ['banana', 'orange', 'strawberry', 'guava']
for (item of arr) {
    console.log(`Item: ${item}`);
}